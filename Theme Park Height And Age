#include <iostream>
using namespace std;
int main()
{
double height;
double age;
    cout << "Please enter your height in meters to check if you are eligible to proceed\n";
    
    cin >> height;
    cout << "Please enter your age to check if you are eligible to proceed\n";
    cin >> age;
    if (age >= 5 && h >= 0.6)
    {
        cout << "You can ride";
    }
    else if (age < 5 && h < 0.5)
    {
        cout << "Sorry you cannot ride";
    }
    else
    {
        cout << "Incorrect input";
    }
    return 0;
}
